**/__pycache__/

# Working capture directories, written by the scripts at the repository root.
# Anchored with a leading slash so they do not also swallow the fixtures the
# tests keep under tests/data/.
/images_calib_cam_*/
/images_world_cam_*/
/images_robot_base_cam_*/
/images_human_base_cam_*/
